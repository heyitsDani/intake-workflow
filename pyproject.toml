[project]
name = "intake-triage-agent"
version = "0.1.0"
description = "Intake triage prototype: classify, route, and abstain on inbound enquiries"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.69.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]
